import { JobHookDirective } from './job-hook.directive';

describe('JobHookDirective', () => {
  it('should create an instance', () => {
    const directive = new JobHookDirective();
    expect(directive).toBeTruthy();
  });
});
